"""Backward-compat shim -- canonical locations: distill.intake.models + distill.intake.services."""

from distill.intake.models import SeedIdea  # noqa: F401
from distill.intake.services import SEEDS_FILENAME, SeedStore  # noqa: F401
